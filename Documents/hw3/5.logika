~( p V s), q V ~r |- r -> q
 {
 1. ~(p V s)   premise
 2. q V ~r      premise
 3. {
    4. r       assume
    5.  {
        6. r -> q        assume
        }
    7. q    ->e 6 4
     }
    8. r -> q       ->i 3
 }
